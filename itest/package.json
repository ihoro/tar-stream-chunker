{
  "name": "tar-stream-chunker.c-itest",
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "test": "cd .. && make clean && make && cd itest && jest",
    "test-watch": "jest --watchAll"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "rxjs": "^6.5.4",
    "uuid": "^3.3.3"
  },
  "jest": {
    "testTimeout": 10000
  }
}
